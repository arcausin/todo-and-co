{{ form_start(form) }}
    <div class="form-group mb-3">
        {{ form_label(form.username, 'Nom d\'utilisateur', {'label_attr': {'class': 'form-label fs-3'}})}}
        {{ form_widget(form.username, {'attr': {'class': 'form-control w-75 text-center mx-auto'}})}}
    </div>

    <div class="form-group mb-3">
        {{ form_row(form.roles) }}
    </div>
    
    <button class="btn text-white">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
