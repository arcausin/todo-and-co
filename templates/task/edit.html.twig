{% extends 'base.html.twig' %}

{% block title %}{{ task.title }} - modifier une tâche{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1 class="text-center mt-3 mb-3">Modifier une tâche</h1>

        <div class="col-12 col-md-6 offset-md-3">
            <div class="text-center border shadow rounded-4 p-4 mb-5">
                {{ include('task/_form.html.twig', {'button_label': 'Modifier'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
