{% extends 'base.html.twig' %}

{% block title %}Accueil - ToDo & Co{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1 class="text-center mt-3 mb-3">Accueil</h1>

        <p class="text-center mb-3">Bienvenue sur l'application <strong>ToDo & Co</strong>, vous pouvez gérer vos tâches.</p>
        
        <div class="text-center mb-3">
            {% if app.user %}
                <a href="{{ path('app_task_index') }}" class="text-white">Voir mes tâches</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="text-white">Se connecter</a> ou <a href="{{ path('app_register') }}" class="text-white">S'inscrire</a>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
