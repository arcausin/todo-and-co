{{ form_start(form) }}
    <div class="form-group mb-3">
        {{ form_label(form.username, 'Nom d\'utilisateur', {'label_attr': {'class': 'form-label fs-3'}})}}
        {{ form_widget(form.username, {'attr': {'class': 'form-control w-75 text-center mx-auto'}})}}
    </div>

    <div class="form-group mb-3">
        {{ form_label(form.roles, 'Rôle', {'label_attr': {'class': 'form-label fs-3'}})}}
        {{ form_widget(form.roles, {'attr': {'class': 'form-select w-75 text-center mx-auto'}})}}
    </div>
    
    <button class="btn text-white border">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
